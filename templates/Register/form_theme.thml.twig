{% block widget_attributes %}

        id="{{ id }}" name="{{ full_name }}"{% if read_only is defined and read_only %} readonly="readonly"{% endif %}{% if disabled %} disabled="disabled"{% endif %}{% if required %} required="required"{% endif %}{% if max_length is defined and max_length %} maxlength="{{ max_length }}"{% endif %}{% if pattern is defined and pattern %} pattern="{{ pattern }}"{% endif %}
        {% for attrname, attrvalue in attr %}{% if attrname in ['placeholder', 'title'] %}{{ attrname }}="{{ attrvalue|trans({}, translation_domain) }}"{% else %}{{ attrname }}="{{ attrvalue }}{% if attrname == "class" %} form-control{% endif %}" {% endif %}{% endfor %}

{% endblock widget_attributes %}